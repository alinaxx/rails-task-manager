<%= form_for(@task) do |f| %>
  <%= f.label :name %>
  <%= f.text_field :name %>

  <%= f.check_box(:status) %>
  <%= f.label(:status, "Done") %>

  <%= f.check_box :description %>
  <%= f.label :description %>

  <%= f.submit %>
<% end %>
